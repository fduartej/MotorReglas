apiVersion: v1
kind: Service
metadata:
  name: cn-ecomerce-frontend
spec:
  selector:
    app: cn-ecomerce-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
