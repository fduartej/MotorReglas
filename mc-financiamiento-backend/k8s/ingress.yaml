apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mc-financiamiento-backend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: mc-financiamiento-backend.contoso.com # Cambia esto por tu dominio
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mc-financiamiento-backend
                port:
                  number: 80
